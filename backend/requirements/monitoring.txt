# Monitoring & Observability Dependencies

# Structured Logging
structlog>=24.1.0
python-json-logger>=2.0.7
colorama>=0.4.6  # 콘솔 로그 색상

# Metrics
prometheus-client>=0.19.0

# Distributed Tracing (Phase 2)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-instrumentation-fastapi>=0.43b0

# Alerting
aiohttp>=3.10.0  # Slack webhook용 (이미 base.txt에 있음)

# Performance Profiling
py-spy>=0.3.14  # CPU 프로파일링 (개발용)
memory-profiler>=0.61.0  # 메모리 프로파일링 (개발용)

